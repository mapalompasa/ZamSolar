# Cloudflare deployment configuration
# Supports both `npx wrangler deploy` (Workers) and `npx wrangler pages deploy` (Pages).

name = "zamsolar"
compatibility_date = "2026-03-01"

# Required for `npx wrangler deploy` to locate the build output directory.
[assets]
directory = "./dist"
